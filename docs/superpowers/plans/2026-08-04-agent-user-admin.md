# Administración de agentes — Plan de implementación

**Goal:** Pantalla Admin TI para CRUD mock de agentes; login por email desde store `localStorage`.

**Spec:** `docs/superpowers/specs/2026-08-04-agent-user-admin-design.md`

## Tasks

1. `users-store` (`netops.users.v1`) + seed
2. Auth/login/shell leen del store
3. Ruta `/usuarios` + UI lista/form
4. Peers chat interno desde store
5. Build + commit local (sin push)
